<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      .navbar {
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: purple;
        
      }
      .navbar a {
        color: white;
        text-decoration: none;
        padding: 10px;
      }
    </style>
  </head>

  <body>
     <div class="navbar">
      <a href="/">Home</a>
      <div>
        <a href="/articles">Articles</a>
        <%= link_to "New Article", new_article_path %>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { turbo_confirm: "You really want to logout?", turbo_method: :delete } %>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
